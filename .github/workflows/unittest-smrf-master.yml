name: Unittest - SMRF master

on:
  pull_request:
    types: [labeled]

permissions:
  contents: read

jobs:
  unittest:
    if: github.event.label.name == 'test-smrf-master'
    uses: ./.github/workflows/test_base.yml
    with:
      smrf_ref: master
